#!/bin/bash

#include <stdio.h>
 
/* main serves as the entry point function in C */
 /* return always turns to zero if there are no errors else it gives some value */

int main ()
	{
	/* puts variable prints the value inside the inverted commas. */
		/* the \n after the variable indicates anotherline after the printed value */
	puts ( "\"Programming is like building a multilingual puzzle" );
	return 0;
	}  
